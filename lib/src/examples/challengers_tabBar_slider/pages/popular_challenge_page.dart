import 'package:flutter/material.dart';

class PopularChallengePage extends StatelessWidget {
  const PopularChallengePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("인기 챌린지 페이지"));
  }
}
