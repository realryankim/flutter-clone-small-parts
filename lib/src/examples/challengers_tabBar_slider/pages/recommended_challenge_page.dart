import 'package:flutter/material.dart';

class RecommendedChallengePage extends StatelessWidget {
  const RecommendedChallengePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("추천 챌린지 페이지"));
  }
}
