import 'package:flutter/material.dart';

class NewChallengePage extends StatelessWidget {
  const NewChallengePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("신규 챌린지 페이지"));
  }
}
